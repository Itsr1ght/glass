package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "glass",
    srcs = glob(include=["**/*.h","**/*.cc"]),
    data = ["./res/"],
    deps = [
        "@//third_party/glfw",
        "@//third_party/glad",
    ],
)
